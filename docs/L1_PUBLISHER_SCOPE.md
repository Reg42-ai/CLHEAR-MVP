# CLHEAR L1 publisher scope

L1 is a company-independent regulatory corpus. Organization applicability belongs downstream; an organization's products, licences or demo needs never remove publisher documents from L1's denominator.

The scope covers every current publisher profile in `app/clhear/l1/publishers.py`: rules and legislation, financially relevant standards, official compliance guidance, rule filings and amendments, examination and enforcement publications, and their official attachments. It includes current material and officially exposed historical archives. It excludes unavailable historical reconstruction, unrelated news/research/speeches, company-filing databases, and unrelated industry standards. Relevant standards cover financial services, compliance, governance, reporting, security, privacy and resilience.

`source_registry.py` contains the known starting declarations. It is not a complete publisher inventory. The former `registry_etoro.py` import path remains compatible but points to these neutral declarations. Old research is retained under `docs/archive/`; its company-specific scope restrictions are inactive.

## Evidence and worker execution

Only L0/L1 system workers may discover, reconcile, acquire, parse or evaluate source content. Readers do not run discovery. `run_inventory_audit(..., scope="all_publishers", discover=True)` uses the same implementation as daily worker discovery. `all_publishers` and the legacy `registered` name address the same global audit stream.

Each discovery cycle persists a publisher/profile/date identity and leased page frontier. Successful page checkpoints retain the exact URL, source permission evidence, artifact hash and publisher-check timestamp. Interrupted batches resume; duplicate claims are fenced. Catalog pagination retains the reviewed catalog permission identity; constituent documents require their own operation grants. Permission checks occur before fetching and again before storing/parsing a returned response. Failures never refresh a prior publisher check.

The persisted cycle date identifies a resumable daily traversal; it is not a publisher-wide historical as-of filter. Catalogs can change during that traversal. A consistent publication cutoff and archive/language reconciliation remain acceptance gaps until the applicable publisher contract supports them.

A bounded batch is not a complete crawl. Remaining pages, unknown publication types, unsupported dynamic filters, alternate hosts, unavailable originals and permission failures remain explicit gaps. The global document total is `null` until the whole publisher inventory has been enumerated and independently reviewed. Known documents and blocked dependencies remain visible, without claiming the known count is the full denominator.

## Implemented catalog contracts

All **45 current publisher profiles** now select executable discovery code: **1 FINRA catalog**, **3 typed Cellar/ESMA/GovInfo protocols**, **2 new UK Atom/Australian OData protocols**, and **39 publisher-specific HTML/attachment library contracts**. This replaces the previous nine partial implementations and 36 missing publisher implementations. It does **not** mean 45 inventories are complete or 45 sources are imported. Each retains explicit residual coverage gaps. None of this implementation has run a live corpus import; genuine acceptance evidence must come from the deployed L1 cycle after review and merge.

Finite catalog progress includes actual next-page links (including pagination navigation), observed bounded GET form combinations, exposed archive/year links, original attachment URLs and language alternatives. Unknown form parameters, POST/JavaScript-only controls, empty pages, external attachment hosts, access challenges and byte/page limits become explicit findings; they cannot certify exhaustion. No years or document URLs are generated merely from a title. The Australian documented composite-document and EUR-Lex documented CELEX URL forms are constructed only from publisher-supplied identifiers.

- **FINRA:** fourteen Manual/rules/archive/filing/notice/guidance/report/enforcement categories, now including FAQs, NAC/OHO adjudications and sanction guidelines. Non-rule publications and attachments retain their own parser and permission identity. Actual numeric page/year links and finite GET year options are resumable. `finra_enforcement_search_contract_required` remains for Disciplinary Actions Online and tool-hosted filing status, independently of published monthly actions and decisions. [Official catalog](https://www.finra.org/rules-guidance/notices) states that pre-1995 notices require contacting FINRA; unavailable history is not reconstructed.
- **EU Publications Office:** ordered CELEX keyset enumeration and CDM language-expression metadata. A publisher-declared authoritative English expression satisfies original plus English; other expression metadata is preserved without importing every equal-authoritative language duplicate. When English is absent, one identified original language remains a separate source for the English-view workflow. Exact language URLs prevent shared-URL cache confusion; returned language and legal-body structure must match. Unknown expressions emit `cellar_language_expression_unverified` or `cellar_language_inventory_missing`; wider category/manifestation reconciliation still emits `publisher_categories_unconfigured`. [Official CDM/SPARQL documentation](https://op.europa.eu/en/web/cellar/cellar-data/metadata/knowledge-graph). Publisher challenge pages fail rather than becoming document text.
- **ESMA:** typed paginated [official library](https://www.esma.europa.eu/databases-library/esma-library?page=0), exact row totals and official linked PDFs. Excluded speeches/news stay out; ambiguous types and changed page sizes are explicit findings. `publisher_categories_unconfigured` retains archive/attachment/category reconciliation.
- **GovInfo:** [official sitemaps](https://www.govinfo.gov/sitemaps) enumerate exposed USCODE, annual CFR and Federal Register years/packages, then exact linked originals. Package/granule reconciliation and other required categories retain `publisher_categories_unconfigured`. [GPO protocol](https://github.com/usgpo/sitemap).
- **UK legislation:** the documented all-instruments Atom feed follows actual `rel=next` URLs, rather than the newest-only feed that omits silently added historical instruments. XML external entities, invalid instrument identities and backward cursors are rejected. `publisher_history_reconciliation_required` retains point-in-time versions, Welsh expressions and effects/attachments. [Official search API](https://legislation.github.io/data-documentation/api/search.html).
- **Australian legislation:** official OData document enumeration uses projected metadata (no document byte fields), declared totals and ordered offsets. Exact composite keys preserve dates, rectifications, document types and volumes; originals are acquired later by their source tasks. `publisher_history_reconciliation_required` retains count stability and non-PDF-only attachments. [Official reuse/API guidance](https://www.legislation.gov.au/help-and-resources/using-the-legislation-register/data-share-and-reuse) and [OpenAPI schema](https://api.prod.legislation.gov.au/swagger/v1/swagger.json). Operational scheduling must respect the publisher's crawling guidance; this code adds no independent crawler.

### Remaining library-contract gaps

Each following publisher has real route-specific enumeration, followed by its own residual finding. `implementation_and_scope_review` means CLHEAR must further reconcile categories or document the API/schema; it is not a licensing excuse. `access_and_contract` also needs publisher access, artifact or rights evidence. All catalog acquisition still requires actual operation evidence, including public catalogs; a technical implementation never grants rights.

- **fca** — [publications](https://www.fca.org.uk/publications). Finding `fca_catalog_reconciliation_required` (`access_and_contract`): The authenticated Handbook API and its separately exposed historical editions require a reviewed API credential/contract.
- **sec** — [rules](https://www.sec.gov/rules-regulations/rulemaking-activity); [sro](https://www.sec.gov/rules-regulations/self-regulatory-organization-rulemaking); [enforcement](https://www.sec.gov/enforcement-litigation). Finding `sec_catalog_reconciliation_required` (`implementation_and_scope_review`): Staff interpretive guidance, examination risk-alert catalogs and pre-modern rulemaking archive category reconciliation remain required.
- **fincen** — [guidance](https://www.fincen.gov/resources/statutes-regulations/guidance); [rulings](https://www.fincen.gov/resources/statutes-regulations/administrative-rulings). Finding `fincen_catalog_reconciliation_required` (`implementation_and_scope_review`): Alerts, enforcement actions, special measures and Federal Register cross-catalog reconciliation remain required.
- **irs** — [bulletins](https://www.irs.gov/irb). Finding `irs_catalog_reconciliation_required` (`implementation_and_scope_review`): Separate tax forms/instructions and official guidance outside Internal Revenue Bulletins need category reconciliation.
- **ofac** — [programs](https://ofac.treasury.gov/sanctions-programs-and-country-information); [faqs](https://ofac.treasury.gov/faqs/all-faqs); [archives](https://ofac.treasury.gov/archive-inactive-sanctions-programs). Finding `ofac_catalog_reconciliation_required` (`implementation_and_scope_review`): Civil penalties, licenses and non-SDN list datasets require explicit category/data-schema reconciliation.
- **nydfs** — [regulations](https://www.dfs.ny.gov/industry_guidance/regulations). Finding `nydfs_catalog_reconciliation_required` (`implementation_and_scope_review`): Enforcement orders and historical predecessor agency archive reconciliation remain required.
- **nasdaq** — [rules](https://listingcenter.nasdaq.com/rulebook/nasdaq/rules). Finding `nasdaq_catalog_reconciliation_required` (`implementation_and_scope_review`): POST/search-driven rule-filing pagination and disciplinary catalogs need a documented endpoint contract.
- **nist** — [publications](https://csrc.nist.gov/Publications). Finding `nist_catalog_reconciliation_required` (`implementation_and_scope_review`): Search-only withdrawn editions and mixed research publication types need catalog row reconciliation; unrelated research is excluded.
- **cysec** — [legislation](https://www.cysec.gov.cy/en-GB/legislation/investment-services/). Finding `cysec_catalog_reconciliation_required` (`implementation_and_scope_review`): Non-investment-services categories and enforcement archives require category reconciliation; English translation authority remains document-specific.
- **asic** — [guides](https://asic.gov.au/regulatory-resources/find-a-document/regulatory-guides/). Finding `asic_catalog_reconciliation_required` (`implementation_and_scope_review`): Enforcement and examination catalogs require separate publication-category reconciliation.
- **austrac** — [guidance](https://www.austrac.gov.au/industry-and-business/obligations-and-guidance); [archive](https://www.austrac.gov.au/business/how-comply-and-report-guidance-and-resources/guidance-resources/all-resources). Finding `austrac_catalog_reconciliation_required` (`implementation_and_scope_review`): Enforcement notices and historical AML/CTF instruments need category reconciliation.
- **sg-law** — [acts](https://sso.agc.gov.sg/Browse/Act/Current); [subsidiary](https://sso.agc.gov.sg/Browse/SL/Current); [gazette](https://sso.agc.gov.sg/Browse/SL-Supp). Finding `sg_law_catalog_reconciliation_required` (`implementation_and_scope_review`): POST-only browse filters and point-in-time history not exposed as links remain unresolved.
- **mas** — [regulation](https://www.mas.gov.sg/regulation). Finding `mas_catalog_reconciliation_required` (`access_and_contract`): Publisher currently presents a maintenance/access page to research requests; no bypass or complete inventory is claimed.
- **sg-privacy** — [guidance](https://www.pdpc.gov.sg/guidelines-and-consultation). Finding `sg_privacy_catalog_reconciliation_required` (`implementation_and_scope_review`): Commission decision pagination and archived consultation closure documents require reconciliation.
- **adgm** — [rules](https://www.adgm.com/legal-framework/rules-and-regulations); [guidance](https://www.adgm.com/legal-framework/guidance-and-policy-statements). Finding `adgm_catalog_reconciliation_required` (`implementation_and_scope_review`): ADGM rulebook historical effective-date controls need an exact provider contract; UAE English translations are not authoritative originals.
- **uae-law** — [arabic](https://uaelegislation.gov.ae/ar); [english](https://uaelegislation.gov.ae/en). Finding `uae_law_catalog_reconciliation_required` (`implementation_and_scope_review`): Dynamic category filters and repealed historical editions are not an exhausted archive; English counterpart authority requires explicit evidence.
- **seychelles** — [legislation](https://fsaseychelles.sc/legal-framework/legislation). Finding `seychelles_catalog_reconciliation_required` (`implementation_and_scope_review`): Enforcement and superseded-document archive categories require reconciliation.
- **malta-law** — [legislation](https://legislation.mt/). Finding `malta_law_catalog_reconciliation_required` (`implementation_and_scope_review`): Dynamic official catalog API and full historical edition traversal require a verified response schema.
- **mfsa** — [circulars](https://www.mfsa.mt/publications/circulars/). Finding `mfsa_catalog_reconciliation_required` (`implementation_and_scope_review`): Rulebooks, decisions and JS-only circular date filters need category and pagination reconciliation.
- **fiau** — [procedures](https://fiaumalta.org/procedures-guidance-2/). Finding `fiau_catalog_reconciliation_required` (`implementation_and_scope_review`): Administrative enforcement and superseded implementing-procedure archives require reconciliation.
- **gibraltar** — [library](https://www.fsc.gi/downloads?section=9&type=0); [aml](https://www.fsc.gi/AMLCFTCPF_guidance_notes). Finding `gibraltar_catalog_reconciliation_required` (`implementation_and_scope_review`): GFSC decisions and legislation-site archive enumeration need category reconciliation; unrelated speeches are excluded.
- **isa** — [legacy-library](https://www.isa.gov.il/sites/ISAEng/1489/1511/Pages/default.aspx). Finding `isa_catalog_reconciliation_required` (`implementation_and_scope_review`): The legacy English collection does not enumerate the current Hebrew ISA publication catalog; new portal schema/archives remain unresolved.
- **il-privacy** — [authority](https://www.gov.il/he/departments/the_privacy_protection_authority). Finding `il_privacy_catalog_reconciliation_required` (`implementation_and_scope_review`): Gov.il dynamic collectors need a reviewed authority-specific API contract; links to other government departments are not assumed in scope.
- **un** — [sanctions](https://main.un.org/securitycouncil/en/sanctions/information); [archives](https://main.un.org/securitycouncil/en/content/repertoire/sanctions-and-other-committees). Finding `un_catalog_reconciliation_required` (`implementation_and_scope_review`): Resolution symbol/language identity and terminated committee archives require structured UN document metadata; all linked language versions are retained.
- **eu-sanctions** — [sanctions](https://finance.ec.europa.eu/eu-and-world/sanctions-restrictive-measures_en). Finding `eu_sanctions_catalog_reconciliation_required` (`implementation_and_scope_review`): Sanctions-map legal-act relationships and historical official consolidated-list datasets need structured reconciliation.
- **ofsi** — [authority](https://www.gov.uk/government/organisations/office-of-financial-sanctions-implementation). Finding `ofsi_catalog_reconciliation_required` (`implementation_and_scope_review`): Complete OFSI publication ownership and former list replacement/history need GOV.UK metadata API reconciliation.
- **fatf** — [publications](https://www.fatf-gafi.org/en/publications.html); [french](https://www.fatf-gafi.org/fr/publications.html); [guidance](https://www.fatf-gafi.org/en/guidance.html). Finding `fatf_catalog_reconciliation_required` (`implementation_and_scope_review`): Search-only pagination and separation of compliance guidance from unrelated research require typed-result reconciliation.
- **basel** — [publications](https://www.bis.org/bcbs/publications.htm); [framework](https://www.bis.org/basel_framework/). Finding `basel_catalog_reconciliation_required` (`implementation_and_scope_review`): Framework effective-date selections and publication status/category totals require explicit historical reconciliation.
- **iosco** — [reports](https://www.iosco.org/publications/?subsection=public_reports). Finding `iosco_catalog_reconciliation_required` (`implementation_and_scope_review`): IOSCO v2 dynamic response schema and excluded research-type rows require typed catalog validation.
- **wolfsberg** — [standards](https://wolfsberg-group.org/resources/general). Finding `wolfsberg_catalog_reconciliation_required` (`implementation_and_scope_review`): Historical withdrawn versions not linked from the resources index need publisher catalog reconciliation.
- **iso** — [security](https://www.iso.org/committee/45306/x/catalogue/); [financial-services](https://www.iso.org/committee/49650/x/catalogue/). Finding `iso_catalog_reconciliation_required` (`access_and_contract`): Governance, reporting and resilience committee boundaries need explicit financial-domain review; paid originals/amendments require authorized artifacts and processing rights.
- **aicpa** — [soc](https://www.aicpa-cima.com/topic/audit-assurance/audit-and-assurance-greater-than-soc-2). Finding `aicpa_catalog_reconciliation_required` (`access_and_contract`): Broader assurance standards and archived editions require authenticated publisher catalog access; free account access does not establish processing rights.
- **pci** — [documents](https://www.pcisecuritystandards.org/document_library/); [standards](https://www.pcisecuritystandards.org/standards/). Finding `pci_catalog_reconciliation_required` (`access_and_contract`): Document-library acceptance/download tokens and retired-edition filters require a reviewed publisher contract; standards landing pages never count as originals.
- **ifrs** — [standards](https://www.ifrs.org/issued-standards/list-of-standards/). Finding `ifrs_catalog_reconciliation_required` (`access_and_contract`): Standards Navigator authentication, translations and historical digital editions require licensed catalog/artifact access; public summaries are not standards text.
- **be-fsma** — [french](https://www.fsma.be/fr/intermediaire); [english](https://www.fsma.be/en/intermediary); [dutch](https://www.fsma.be/nl/publiciteitsverplichtingen). Finding `be_fsma_catalog_reconciliation_required` (`implementation_and_scope_review`): Non-intermediary financial-sector categories, sanction decisions and complete multilingual circular catalogs require reconciliation.
- **fr-amf** — [regulation](https://www.amf-france.org/fr/reglementation/reglementation-accueil). Finding `fr_amf_catalog_reconciliation_required` (`implementation_and_scope_review`): General Regulation point-in-time versions, policy version links and sanctions catalogs require typed reconciliation.
- **es-cnmv** — [regulation](https://www.cnmv.es/Portal/Menu/Legislacion?lang=es); [circulars](https://www.cnmv.es/Portal/Legislacion/Circulares?lang=es). Finding `es_cnmv_catalog_reconciliation_required` (`implementation_and_scope_review`): Sanction decisions, archive year filter parameters and translation legal status require reconciliation.
- **de-bafin** — [rules](https://www.bafin.de/DE/RechtRegelungen/Rundschreiben/rundschreiben_node.html). Finding `de_bafin_catalog_reconciliation_required` (`implementation_and_scope_review`): Current portal route migration, revoked circular catalogs and enforcement measures require primary endpoint confirmation.
- **it-consob** — [mifid](https://www.consob.it/web/area-pubblica/mifid-2); [intermediaries](https://www.consob.it/web/area-pubblica/intermediari-normativa-nazionale-secondaria). Finding `it_consob_catalog_reconciliation_required` (`access_and_contract`): Publisher currently presents CAPTCHA/bot protection; no bypass. Wider regulation and sanctions archive categories remain unresolved.

### Original language evidence

Discovery records observed language alternatives without guessing authority from URLs or ASCII. A worker can certify native publisher HTML language only from actual `html lang` metadata, an identified document body and the exact artifact SHA, then binds it to the imported version. Multipart, PDF, missing language metadata and labelled unofficial translations receive no automatic language assertion. Publisher-specific English views and authorized translation are separate from verbatim acquisition and cannot rewrite the originals. Paid standard catalog/product summaries remain `authorized_standard_artifact_required`; they are never counted as standards text, even behind sign-in.

## Collection and document identities

The legacy FINRA Rulebook, NYDFS/CySEC/Seychelles/ISA libraries, Wolfsberg collection, and PCI/IFRS catalogs are collection records. Stored historical versions remain preserved but do not count as imports of their constituent documents. PCI and IFRS require explicit individual document/edition inventories. ISO 27001:2022 and its 2024 amendment remain separate documents; AICPA TSC retains its exact edition identity.

The five national-overlay references no longer fetch one shared ESMA PDF while claiming five national originals. Their source IDs remain stable and their missing official inventories remain visible. Other previously bundled names now describe the actual fetched document; omitted sibling publications belong in publisher discovery, not in inflated source labels.

## Acceptance

Every expected original must have current operation permission, exact edition/provenance evidence, preserved artifact hashes and a complete independently verified encoded projection. The inventory auditor calls the shared original/projection verifier; unsupported formats fail explicitly. Exact wording alone does not prove hierarchy, record multiplicity, complete clause sets or accurate source locations.

One verified source does not certify its publisher, and one reviewed publisher does not certify global L1. Acceptance requires complete publisher coverage, current version and permission bindings, fresh successful publisher checks, and verified wording/order/hierarchy/locations across every expected document. Only actual scheduled worker cycles establish daily scheduling evidence. Manual verification is labelled separately. Failed or incomplete cycles retain the last accepted release.
